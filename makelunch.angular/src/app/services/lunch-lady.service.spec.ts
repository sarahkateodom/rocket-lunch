import { TestBed, inject } from '@angular/core/testing';

import { LunchLadyService } from './lunch-lady.service';

describe('LunchLadyService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [LunchLadyService]
    });
  });

});
